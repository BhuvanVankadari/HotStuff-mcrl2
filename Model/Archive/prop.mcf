% once a correct replica voted for a phase, it cannot go back to a previous phase. 
% The protocol has to make progress.
[true*] forall msg1: Message, replica1, leader1: Pos . 
[message_sent(msg1, replica1, leader1)]
[true*] forall msg2: Message, replica2, leader2: Pos .
val((replica1 == replica2) && (type(msg1)== preCommit && type(msg2) == prepare && viewNumber(msg1) == viewNumber(msg2)) ||
    (type(msg1)== commit && (type(msg2) == prepare || type(msg2) == preCommit) && viewNumber(msg1) == viewNumber(msg2)) ||
    (type(msg1)== decide && (type(msg2) == prepare || type(msg2) == preCommit || type(msg2) == commit) && 
    viewNumber(msg1) == viewNumber(msg2)) ) => [message_sent(msg2, replica2, leader2)] false